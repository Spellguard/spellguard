[project]
name = "spellguard-ctls"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=44.0.0",
    "httpx>=0.28.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
