[project]
name = "spellguard-amp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=44.0.0",
    "spellguard-ctls>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
spellguard-ctls = { path = "../../ctls/py", editable = true }
